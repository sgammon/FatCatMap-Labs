## Layout - Dialogs

class Dialog extends LayoutElement
	
	defaults:
		modal: false
		opacity: true
		autoScale: true
	
	next: ->
		return
		
	previous: ->
		return
		
	goto: (index) ->
		return
		
	cancel: ->
		return
		
	resize: ->
		return
		
	center: ->
		return
		
	title: ->
		return
		
		
window.Layout.Dialog = Dialog