(function() {
  var StorageDriver;
  StorageDriver = (function() {
    function StorageDriver(name, path, config) {
      this.name = name;
      this.path = path;
      this.config = config;
    }
    return StorageDriver;
  })();
}).call(this);
