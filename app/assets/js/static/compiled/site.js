(function() {
  var SiteSection;
  SiteSection = (function() {
    function SiteSection(name, path, config) {
      this.name = name;
      this.path = path;
      this.config = config;
    }
    return SiteSection;
  })();
}).call(this);
