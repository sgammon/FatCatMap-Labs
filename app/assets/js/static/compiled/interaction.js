(function() {
  var InteractiveWidget;
  InteractiveWidget = (function() {
    function InteractiveWidget(name, path, config) {
      this.name = name;
      this.path = path;
      this.config = config;
    }
    return InteractiveWidget;
  })();
}).call(this);
